import React, { useEffect, useState } from 'react'
import axios from "axios"
function Follower({ followUser,mainUser }) {
  const [followeid, setFollowerid] = useState(null);
 
 
 
  

  return <>
    
  </>
}

export default Follower